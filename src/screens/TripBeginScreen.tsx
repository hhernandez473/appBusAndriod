import React from 'react'
import { View } from 'react-native';
import { Text } from 'react-native-paper';

export const TripBeginScreen = () => {
    return (
        <View>
            <Text> Inicio de viaje</Text>
        </View>
    )
}
